#!/bin/bash

set -e

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"

if [ -L $DIR/configure ]; then
  CONFDIR=`dirname $DIR/$(readlink $DIR/configure)`
else
  CONFDIR="$DIR"
fi

ts-node --transpile-only "$CONFDIR/index.ts" $@
