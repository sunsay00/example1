import { IUserContext } from '../types';
declare class TestingService {
    privateUpperCase(user: IUserContext, arg: string): string;
    protectedUpperCase(user: IUserContext, arg: string): string;
    publicUpperCase(user: IUserContext, arg: string): string;
}
export default TestingService;
//# sourceMappingURL=testingservice.d.ts.map